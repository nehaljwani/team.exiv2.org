#!/bin/bash

c="import time; import string; print(   time.strftime('%Y-%m-%d %H:%M:%S', time.localtime()).lower()    )"
python3 -c "$c" | tr -d '\n' | pbcopy ; pbpaste ; echo

# That's all Folks! 
##
