#!/usr/bin/perl

my $count = 3 ;
while ( $count ) {
	my $seconds = $ARGV[0] ;
	my $cmd     = $ARGV[1] ;
#	print "cmd = $cmd " . " seconds = $seconds " . "\n" ;
	system($cmd) ;
	system("sleep $seconds") ;
}

	