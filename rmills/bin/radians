#!/usr/bin/env perl

if ( $#ARGV < 0 ) {
	printf ("syntax: $0 radians+\n");
	exit(1);
}

for ( $i = 0 ; $i <= $#ARGV ; $i++ ) { 
	my $degree=$ARGV[$i];
	my $radian=atan2(1,1) * $radian /45.0 ;
	printf("%.3f\n",$radian);
}

# That's all Folks
##
